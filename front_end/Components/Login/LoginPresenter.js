import React from 'react';
import { 
    View, 
    Text
} from 'react-native'

const LoginPresenter = (props) => {
    return (
        <View>
            <Text>Login</Text>
        </View>
    );
};

export default LoginPresenter;