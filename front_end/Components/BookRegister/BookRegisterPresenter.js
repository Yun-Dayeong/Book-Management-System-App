import React from 'react';
import {
    View, 
    Text
} from 'react-native'

const BookRegisterPresenter = (props) => {
    return (
        <View>
            <Text>Book Register</Text>
        </View>
    );
};

export default BookRegisterPresenter;